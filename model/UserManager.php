<?php

class UserManager extends Manager

{
   /* public function exists($id)
       $q =  $this->db->prepare('SELECT id FROM users WHERE id = ?');
        $q->execute(array($id));

        return (bool)$q->rowCount();

        localhost/index.php?action="truc"
   */
}